{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && git config --global --add safe.directory '*' && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter config --no-analytics && flutter config --enable-web && flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null
}
